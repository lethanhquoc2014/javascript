// return the page before the previous page
window.history.go(-2);

// To go forward three pages
window.history.go(3);

// equivalent
window.go(-1);
window.back();

// equivalent
window.go(1);
window.forward();
